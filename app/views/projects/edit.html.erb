<div>
  <%= render BreadcrumbsComponent.new(@project, "Edit") %>
  <div class="project">
    <%= page_header @project %>
    <%= turbo_frame_tag Project.new do %>
      <%= render partial: "form", locals: {project: @project} %>
    <% end %>
  </div>
</div>
