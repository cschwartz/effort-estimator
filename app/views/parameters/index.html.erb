<%= render Layout::IndexViewComponent.new(
  resource_class: Parameter,
  collection: @parameters,
  row_component: Table::Rows::ParameterRowComponent,
  headers: ["Title", "Actions"],
  new_path: new_project_parameter_path(@project),
  parent_resources: [@project]
) %>
