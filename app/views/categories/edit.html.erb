<div>
  <%= render BreadcrumbsComponent.new(@project, @category, "Edit") %>
  <div class="category">
    <%= render PageHeaderComponent.new(@category) %>
    <%= turbo_frame_tag Category.new do %>
      <%= render partial: "form", locals: {project: @project, category: @category} %>
    <% end %>
  </div>
</div>
