<div id="<%= category.title.parameterize %>-parameter-display" class="alert alert-info mb-4">
  <%= render IconComponent.new(name: :info, css_class: "shrink-0") %>
  <div>
    <h4 class="font-bold"><%= category.title %>: per <%= parameter.title %></h4>
  </div>
</div>
