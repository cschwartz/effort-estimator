<div class="p-4 flex flex-col h-full">
  <h3 class="text-lg font-semibold mb-4"><%= @title %></h3>
  <ul class="<%= menu_class %> grow">
    <% items.each do |item| %>
      <%= item %>
    <% end %>
    <li class="mt-auto logout">
      <%= button_to(
        "Log Out",
        destroy_user_session_path,
        method: :delete
      ) %>
    </li>
  </ul>
</div>
